<h1>Best D@mn Burger!</h1>

<h2>Submit the best burger you've ever had.</h2>
<h4>Hungry? Click the "DEVOUR!" button to put that bad boy away.</h4>

<ul>
    {{#each burgers}}
        {{#if devoured}}
        {{!-- What is this line doing? Partials? --}}
         {{> burgers/burger-block devoured=true}}
        {{/if}}
    {{/each}}
</ul>

<ul>
    {{#each burgers}}
        {{#unless burgers}}
        {{!-- What is this line doing? --}}
            {{> burgers/burger-block devoured=false}}
        {{/unless}}
     {{/each}}   
</ul>



<h2>Add a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="bu">Burger Name:</label>
        <input type="text" id="bu" name="name">
    </div>

    <button type="submit">Add burger</button>   

</form>